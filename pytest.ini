# File: pytest.ini (located in the project root directory)
[pytest]
# Add the current directory (.) to the Python search path
pythonpath = .
markers =
    integration: mark a test as an integration test.
